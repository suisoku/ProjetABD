Here all the stuff concerning the building of the database

FILE BD.sql : Containing all the tables with static constraints
FILE TRIGGERS.sql : Should contain all the triggers of the DB
FILE POPULATE.sql : Should contain a minimum set of inserts 